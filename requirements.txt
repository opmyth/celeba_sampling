# PyTorch must be installed separately with the appropriate CUDA variant:
#   pip install torch==2.11.0+cu128 torchvision==0.26.0+cu128 \
#       --index-url https://download.pytorch.org/whl/cu128
# Adjust the cu128 suffix to match the CUDA version on your cluster.
torch>=2.0.0
torchvision>=0.15.0

# Core scientific / sampling dependencies
numpy>=1.24.0
scipy>=1.10.0
POT>=0.9.0
tqdm>=4.60.0

# Plotting (used in test_clf.py)
matplotlib>=3.5.0
pillow>=9.0.0

# Experiment tracking — run `wandb login` or set WANDB_API_KEY before running
wandb
